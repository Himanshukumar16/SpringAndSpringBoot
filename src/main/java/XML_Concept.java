public class XML_Concept {
    /*
    XML terms :->
        groupID Should be unique.
        ArtifactId is the project Name.
        and version is the version of dependencies....

    we can add dependencies here by using dependencies tag....
    and connect to any dependency.... like shown below:

    <dependencies>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>9.3.0</version>
        </dependency>
    </dependencies>

    In Maven, an archetype is basically a template for generating a new project.

    Maven completes here !!....
 */

}
